Generate Random array Program takes the input as the No of Test cases(No of Random Arrays to be generated) and then generates random array of sizes upto 25

The executable after taking these input generates the input.txt which is used as input for BinaryVsTernary.exe
